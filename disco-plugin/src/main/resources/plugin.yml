name: Disco
version: 0.0.1-rc0
main: io.disco.plugin.DiscoPlugin
api-version: '26.1.2'
description: PoC plugin proving Paper + Rust feasibility via paper-loader.
author: Austin Gill
# commands are registered programmatically from Rust via the CommandMap; no
# `commands:` section needed here.
